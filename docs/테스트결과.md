# 테스트 결과 — DeviceLink

`DeviceLink.Tests` (xUnit) · 실행: `dotnet test tests/DeviceLink.Tests`

## 요약

```
통과! - 실패: 0, 통과: 23, 건너뜀: 0, 전체: 23, 기간: ~135 ms
빌드 경고 0
```

| 영역 | 파일 | 테스트 | 무엇을 보장 |
|---|---|---:|---|
| 코드 매핑 | `MetricCatalogTests` | 8 | metric→LOINC/UCUM 정확성, 대소문자 무시, 미등록 거부 |
| HL7 왕복 | `OruRoundTripTests` | 3 | ORU^R01 생성↔파싱 값 무손실, 표시명 전달, 미등록 metric 제외 |
| 파서 견고화 | `OruParserTests` | 3 | 깨진/비-ORU(ADT) 거부, 숫자 아닌 OBX만 건별 스킵 |
| MLLP 프레이밍 | `MllpReaderTests` | 4 | 단일/다중/분할 프레임, 선행 잡음 무시 |
| FHIR 매핑 | `ObservationMapperTests` | 5 | 스칼라→개별, 혈압 3종→panel+component, subject/device |

> 네트워크·HAPI 서버에 의존하지 않는 **순수 단위 테스트**다(결정론적, ~0.1초).
> `FhirClient` POST와 `PatientRegistry`(서버 검색/생성)는 외부 서버가 필요해 단위 테스트에서 제외 —
> 이쪽은 종단 수동 검증(서버 되조회)으로 확인했다(README 참조).

## 통과한 테스트 (23)

**MetricCatalogTests**
- `TryGet_known_metric_returns_loinc_and_ucum` — HR/SpO2/TEMP/NIBPs/NIBPd/NIBPm (6 케이스)
- `TryGet_is_case_insensitive`
- `TryGet_unknown_metric_returns_false`

**OruRoundTripTests**
- `Build_then_parse_preserves_all_obx_values` — 6 OBX(혈압 3종 포함) 값/단위/장치/시각/환자 보존
- `Build_carries_display_name_from_catalog`
- `Unknown_metric_is_skipped_when_building`

**OruParserTests**
- `Garbage_message_throws`
- `Non_oru_message_throws_NotSupported` — ADT^A01 거부
- `Non_numeric_obx_value_is_skipped` — 한 OBX만 버리고 나머지 유지

**MllpReaderTests**
- `Reads_single_frame`
- `Reads_two_frames_from_one_buffer` — 한 수신에 두 메시지
- `Reassembles_frame_split_across_reads` — 한 메시지가 여러 read로 쪼개짐
- `Ignores_bytes_before_start_block`

**ObservationMapperTests**
- `Scalar_obx_maps_to_single_observation` — code/value/unit/category/subject/device
- `Three_bp_obx_collapse_into_one_panel_with_components` — 85354-9 + component 3개
- `Scalars_and_bp_mix_yields_scalars_plus_one_panel`
- `Bp_panel_built_even_if_mean_missing` — MAP 없어도 panel
- `Null_subject_is_allowed`

## 재현

```bash
export DOTNET_ROOT="/opt/homebrew/opt/dotnet@8/libexec"
export PATH="/opt/homebrew/opt/dotnet@8/bin:$PATH"
dotnet test tests/DeviceLink.Tests
```
