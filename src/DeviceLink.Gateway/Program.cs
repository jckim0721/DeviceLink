// DeviceLink.Gateway — TCP 수신 → Reading 파싱 → FHIR Observation 변환 → FHIR 서버 POST
// TODO(M1 Day3~5): TCP 리스너 → 파싱 → Firely 매핑 → FhirClient POST

Console.WriteLine("DeviceLink.Gateway — 스켈레톤. 수신/변환 로직 미구현 (M1 Day3~5 예정).");
